
import PropTypes from 'prop-types';
import Course from './Course';

const Courses = props => {
   return (
      <div>
         <Course/>
      </div>
   );
};

Courses.propTypes = {
   
};

export default Courses;